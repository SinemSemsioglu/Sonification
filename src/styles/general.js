import React from 'react';
import {StyleSheet} from 'react-native'

styles = StyleSheet.create({

  alwaysred: {
    backgroundColor: 'red',
    height: 100,
    width: 100,
  },

});

module.export = {
  styles
}
